{
  "$id": "definition.json",
  "type": "object",
  "properties": {
    "token": {
      "$ref": "tokenSchema"
    }
  },
  "additionalProperties": false,
  "errorMessage": {
    "additionalProperties": "Only token, dataSource, visualization, form and layout are allowed as key"
  }
}