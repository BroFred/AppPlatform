{
  "$id": "definition.json",
  "type": "object",
  "properties": {
    "tokens": {
      "$ref": "tokenSchema"
    },
    "dataSources": {
      "$ref": "dataSourceSchema"
    },
    "visualizations":{
      "$ref": "vizSchema"
    },
    "forms":{
      "$ref": "formSchema"
    },
    "layout": {
      "$ref": "layoutSchema"
    }
  },
  "additionalProperties": false,
  "errorMessage": {
    "additionalProperties": "Only tokens, dataSources, visualizations, forms and layout are allowed as key"
  }
}